{% extends 'base.html.twig' %}

{% block body %}
    <section class="formularios">
        <h2 class="titulo-form">Ingrese nueva cátedra:</h2>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <input class="btn" type="submit" value="Crear cátedra" />
        {{ form_end(form) }}
    </section>

<!--     <ul>
        <li>
            <a href="{{ path('catedra_index') }}">Back to the list</a>
        </li>
    </ul> -->
{% endblock %}
