{% extends 'base.html.twig' %}

{% block body %}
    <section class="formularios">
        <div class="titulo-form">
            <h2><i class="fas fa-plus"></i>Nueva noticia</h2>
        </div>

        {{ form_start(form) }}  
            {{ form_widget(form) }}
            <input type="submit" value="Create" />
        {{ form_end(form) }}

        <ul>
            <li>
                <a href="{{ path('noticia_index') }}">Back to the list</a>
            </li>
        </ul>
    </section>
{% endblock %}
