{% extends 'base.html.twig' %}

{% block body %}
<section class="formularios">
    <h2 class="titulo-form">Editar noticia:</h2>

    {{ form_start(edit_form) }}
        {{ form_row(edit_form.archivo) }}
        <p>Archivo actual: <a href="{{ asset('pdf/'~noticia.archivo) }}">{{ noticia.archivo }}</a></p>
        {{ form_widget(edit_form) }}
        <input class="btn" type="submit" value="Editar noticia" />
    {{ form_end(edit_form) }}
</section>

{% endblock %}
