{% trans_default_domain 'FOSUserBundle' %}

<section class="formularios">
	<div class="fos_user_user_show">
	    <p>Usuario: {{ user.username }}</p>
	    <p>Email: {{ user.email }}</p>
	    <p>Requiere visado: {{ user.email }}</p>
	</div>
</section>